YUI().use("node", "event", "widget", function(Y) {

    // Load our main widget
    
    /*
     * This is an example of the configuration that would be great
     */
    /*
    var editableTable = new Y.EditableTable({
        srcNode:        "#table",
        headerEditable: false,
        addColumn:      false,
        addRow:         false,
        resizable:      false,
    });
    */
});
